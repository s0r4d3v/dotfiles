# Tools

This repo bundles recommended CLI tools. Use shell aliases and modules for usage. See configuration under `modules/home/cli/`.

Common commands:
- `nb` = `nom build`
- `ns` = `nom shell`
- `nd` = `nom develop`
- `initdevenv` = initialize devenv template
