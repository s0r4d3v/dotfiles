# .sops.yaml
# age 公開鍵の設定ファイル
#
# 以下のコマンドで取得した公開鍵に置き換えてください：
#   age-keygen -y ~/.config/sops/age/keys.txt

keys:
  - &primary age1xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx

creation_rules:
  - path_regex: secrets/[^/]+\.yaml$
    key_groups:
      - age:
          - *primary
